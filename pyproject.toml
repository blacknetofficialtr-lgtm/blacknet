[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "warningnet"
version = "1.3.26"
description = "BlackEngine Technology A.Ş. desktop platform"
requires-python = ">=3.10"
dependencies = [
  "PySide6>=6.7",
  "cryptography>=42.0",
]

[project.scripts]
warningnet = "warningnet.app:run"

[tool.pytest.ini_options]
testpaths = ["tests"]
